includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: max
    paths:
        - src
        - config
    tmpDir: build/phpstan
    # The package's own config directory (not the application's config_path()).
    configDirectories:
        - config
